#!/bin/bash

# default unnamed session

tmux start-server

sess=$( tmux new-session -d -P -F '#S' )

tmux \
     neww -t $sess:1 \; \
     neww -t $sess:2 \; \
     neww -t $sess:3 \; \
     selectw -t $sess:0 \; \
     attach -t $sess

tmux set-environment -g LAST_ANON_SESS $sess
